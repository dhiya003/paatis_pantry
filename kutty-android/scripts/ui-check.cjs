const {chromium}=require('playwright');
const assert=require('node:assert/strict');
(async()=>{const browser=await chromium.launch({headless:true,args:['--no-sandbox']});const page=await browser.newPage({viewport:{width:393,height:851},isMobile:true,hasTouch:true});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.addInitScript(()=>{window.testVoice={starts:0,stops:0};window.SpeechRecognition=class {start(){window.testVoice.starts++}stop(){window.testVoice.stops++;this.onresult?.({results:[[{transcript:'shopping list'}]]});this.onend?.()}abort(){}};localStorage.setItem('kutty-samayal-v1',JSON.stringify({version:1,onboarded:true,profile:{name:'Test baby',dob:'2024-01-01',anchor:'2026-09-16',allergies:[]},rules:{},overrides:{},kitchen:{pantry:[],favourites:[],locks:[],prepDone:[]},changes:{},removedIds:[]}))});await page.goto('http://127.0.0.1:4173');await page.getByRole('heading',{name:'A happy little mealtime'}).waitFor();
assert.equal(await page.getByText('Start listening',{exact:true}).count(),0);
await page.getByRole('button',{name:'My dish list',exact:true}).click();await page.getByLabel('Dish names',{exact:true}).fill('Idli\nKeerai sadam\nFamily special');await page.getByRole('button',{name:'Save my dish list'}).click();await page.getByText('Needs recipe · saved, but not scheduled yet').waitFor();assert.equal(await page.getByText('Recipe ready ·',{exact:false}).count(),2);
await page.getByRole('button',{name:'Add ingredients & steps'}).click();assert.equal(await page.getByLabel('Dish name',{exact:true}).inputValue(),'Family special');await page.evaluate(()=>window.dispatchEvent(new Event('native-back',{cancelable:true})));assert.equal(await page.getByRole('dialog').count(),0);
await page.getByRole('button',{name:'Go back'}).click();await page.getByRole('heading',{name:'A happy little mealtime'}).waitFor();
const carousel=page.getByRole('region',{name:'Sides and snacks carousel'});await carousel.scrollIntoViewIfNeeded();assert(await carousel.evaluate(e=>e.scrollWidth>e.clientWidth));const sizes=await page.evaluate(()=>({meal:document.querySelector('.meal-card').getBoundingClientRect().width,side:document.querySelector('.side-card').getBoundingClientRect().width,overflow:document.body.scrollWidth>innerWidth}));assert(Math.abs(sizes.meal-sizes.side)<2);assert(!sizes.overflow);
await page.screenshot({path:'ui-sides.png'});
const mic=page.getByRole('button',{name:'Hold to speak, release to finish'});const point=await mic.boundingBox();await page.mouse.move(point.x+point.width/2,point.y+point.height/2);await page.mouse.down();assert.equal(await mic.getAttribute('aria-pressed'),'true');await page.mouse.up();await page.getByRole('heading',{name:'One list. One less job.'}).waitFor();assert.deepEqual(await page.evaluate(()=>window.testVoice),{starts:1,stops:1});
await page.evaluate(()=>window.dispatchEvent(new Event('native-back',{cancelable:true})));await page.getByRole('heading',{name:'A happy little mealtime'}).waitFor();
await page.getByRole('button',{name:'Toggle Sidebar'}).click();await page.getByRole('button',{name:'Recipe collection',exact:true}).click();await page.getByRole('button',{name:'All (40)',exact:true}).waitFor();assert.equal(await page.locator('.recipe-card').count(),40);await page.locator('.recipe-name').first().click();await page.getByRole('dialog').waitFor();await page.screenshot({path:'ui-detail.png'});await page.evaluate(()=>window.dispatchEvent(new Event('native-back',{cancelable:true})));assert.equal(await page.getByRole('dialog').count(),0);assert.deepEqual(errors,[]);console.log('PASS back stack, native-back dismissal, bulk dish list, 40 recipes, hold/release command, card widths and no overflow');await browser.close()})().catch(e=>{console.error(e);process.exit(1)});
