import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "in.paatispantry.app",
  appName: "Paati's Pantry",
  webDir: "dist",
  android: { backgroundColor: "#fbf7ef" }
};

export default config;
